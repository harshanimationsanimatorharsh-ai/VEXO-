<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#080810">

    <!-- Top Toolbar -->
    <RelativeLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#0E0E1C"
        android:paddingHorizontal="12dp">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_revert"
            app:tint="@color/white" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnUndo"
                android:layout_width="48dp"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_revert"
                android:rotation="-90"
                app:tint="@color/white" />

            <ImageButton
                android:id="@+id/btnRedo"
                android:layout_width="48dp"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_revert"
                android:rotation="90"
                app:tint="@color/white" />
        </LinearLayout>

        <Button
            android:id="@+id/btnExport"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_button"
            android:text="EXPORT"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- Player Preview Area -->
    <FrameLayout
        android:id="@+id/playerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/editorControls"
        android:layout_below="@id/toolbar"
        android:background="#000000">

        <androidx.media3.ui.PlayerView
            android:id="@+id/playerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="false"
            app:resize_mode="fit" />

        <ImageButton
            android:id="@+id/btnPlayPause"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_media_play"
            app:tint="#80FFFFFF" />

        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="16dp"
            android:background="#80000000"
            android:paddingHorizontal="8dp"
            android:text="00:00 / 00:00"
            android:textColor="@color/white"
            android:textSize="12sp" />
    </FrameLayout>

    <!-- Editor Controls (Bottom Half) -->
    <LinearLayout
        android:id="@+id/editorControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#0E0E1C"
        android:orientation="vertical">

        <!-- Info Bar -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:paddingHorizontal="16dp">

            <TextView
                android:id="@+id/tvClipInfo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:text="No clip selected"
                android:textColor="#AAAAAA"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/tvClipCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="Clips: 0"
                android:textColor="#6C63FF"
                android:textSize="11sp"
                android:textStyle="bold" />
        </RelativeLayout>

        <!-- Custom Timeline View -->
        <!-- Note: This must match the package of your EditorActivity.kt -->
        <com.vexo.app.ui.VTimeline
            android:id="@+id/timelineView"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="#0A0A0F" />

        <!-- Tool Tabs (Horizontal Scroll) -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingHorizontal="8dp">

                <Button id="@+id/btnAddClip" style="@style/EditorToolButton" android:text="Add" />
                <Button id="@+id/btnSplit" style="@style/EditorToolButton" android:text="Split" />
                <Button id="@+id/btnDelete" style="@style/EditorToolButton" android:text="Delete" />
                <Button id="@+id/btnSpeed" style="@style/EditorToolButton" android:text="Speed" />
                <Button id="@+id/btnVolume" style="@style/EditorToolButton" android:text="Volume" />
                <Button id="@+id/btnMoveLeft" style="@style/EditorToolButton" android:text="&lt; Move" />
                <Button id="@+id/btnMoveRight" style="@style/EditorToolButton" android:text="Move &gt;" />
            </LinearLayout>
        </HorizontalScrollView>

        <!-- Effects/Filters Containers (Hidden until needed) -->
        <HorizontalScrollView
            android:id="@+id/filterButtonsContainer"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="#151525"
            android:scrollbars="none"
            android:visibility="visible">
            <!-- Populated via FilterUtils.kt -->
            <LinearLayout android:layout_width="wrap_content" android:layout_height="match_parent" android:orientation="horizontal" />
        </HorizontalScrollView>

        <HorizontalScrollView
            android:id="@+id/effectButtonsContainer"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="#151525"
            android:scrollbars="none"
            android:visibility="gone">
            <LinearLayout android:layout_width="wrap_content" android:layout_height="match_parent" android:orientation="horizontal" />
        </HorizontalScrollView>

    </LinearLayout>

</RelativeLayout>
